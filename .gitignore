# Transient run logs (regenerated by scripts/run_all_experiments.sh; the durable
# record of every run is results/*.json).
logs/
logs_runner.txt

# Downloaded and prepared corpora -- large, and reproducible from scripts/prepare_data.py
data/

# Python / build
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# LaTeX build products
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.pdf
